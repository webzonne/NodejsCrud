<form style='text-align:center; margin-bottom:40px' action="/edit/{{task._id}}" method="POST">
  <div>
    <label for="title">Write a title:</label></br>
    <input type="text" name="title" placeholder="title" value="{{task.title}}" id="title" />
  </div>
  </br>
  <div>
     <label for="description">description:</label></br>
    <textarea name="description" id="description">{{task.description}}</textarea>
  </div>
  </br>
  <button>add</button>
</form>